#!/usr/bin/env python3
"Complex types - list of floats"
from typing import List


def sum_list(input_list: List[float]) -> float:
    "Returns the sum of floats in input_list"
    return sum(input_list)
